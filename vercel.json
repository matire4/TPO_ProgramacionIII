{
  "builds": [
    { "src": "api/server.py", "use": "@vercel/python" },
    { "src": "web/**/*", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "api/server.py" },
    { "src": "/(.*)", "dest": "web/$1" }
  ]
}

